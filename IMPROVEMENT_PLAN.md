# Network Server Suite 개선 계획서

작성일: 2026-07-22
대상 프로젝트: `C:\Users\kms6402\Downloads\network-server-suite`

> **상태: 1~4번 항목 모두 구현 완료 (2026-07-22).** 이 문서는 개선 배경과 설계 근거를 남기기 위한 기록으로 유지한다. 실제 구현된 내용/현재 아키텍처는 `CLAUDE.md`를 참고할 것 — 특히 2번 항목은 아래 A안/B안 중 **B안(ARP 기반 실단말 탐지)** 으로 구현되었다.

## 배경

현재 앱은 전반적으로 "시뮬레이션" 위주로 동작한다. 실제 네트워크 장비/PC와 상호작용하는 것처럼 보이지만, 내부적으로는 미리 정의된 프리셋 데이터나 가짜 응답 텍스트를 재생하는 구조다. 요청하신 4가지 항목을 코드 레벨에서 원인을 확인했고, 각각 실제 동작하도록 개선하는 것이 이번 작업의 목표다.

---

## 1. DHCP 바인딩 어댑터가 현재 PC 어댑터로 표시되지 않는 문제

**원인**
- `server.ts`의 `/api/interfaces`(220행)는 이미 `os.networkInterfaces()`로 PC의 실제 어댑터 목록을 정상적으로 반환하고 있다. 문제는 여기가 아니다.
- `dhcpConfig.interfaceName`의 초기값이 `"Ethernet 1 (eth0)"`(47행)이라는 **가짜 프리셋 이름**으로 하드코딩되어 있는데, Windows 실제 어댑터 이름(예: `Wi-Fi`, `이더넷`)과 절대 일치하지 않는다.
- `getInterfaceInfo()`(188행)는 `nets[name]`으로 정확히 이름이 일치하는 항목만 찾기 때문에, 기본값이 일치하지 않으면 항상 `192.168.1.1` 등 가짜 fallback 값으로 빠진다(207~216행).
- 프론트엔드 `DhcpServer.tsx`의 `<select>`는 `config.interfaceName`을 값으로 바인딩하는데, 실제 목록에 없는 값이라 선택된 상태로 표시되지 않는다.

**개선 방향**
- 서버 기동 시(또는 최초 상태 초기화 시) `dhcpConfig.interfaceName`을 하드코딩된 프리셋 대신, `/api/interfaces`가 반환하는 **실제 어댑터 목록의 첫 번째(또는 internal이 아닌 첫 IPv4) 어댑터**로 자동 설정.
- `getInterfaceInfo()`의 가짜 fallback 로직(207~216행) 제거 — 실제 어댑터를 못 찾으면 에러/경고를 명확히 표시하고 가짜 IP를 절대 반환하지 않도록 변경.
- 프론트엔드는 인터페이스 목록 로드가 끝나기 전에는 select를 비활성화하고, 로드 후 현재 config 값이 목록에 없으면 자동으로 첫 번째 실제 어댑터로 보정.

---

## 2. "1클릭 단말 접속 시뮬레이션" 예제 삭제 + 실제 DHCP 할당 단말 표시

**원인**
- `DhcpServer.tsx`(163~186행)의 `simulationPresets` 배열(Eng-Workstation-05, Oracle-Prod-DB, Core-Switch-Backbone, Smart-Printer-3F)과 "1클릭 시뮬레이트" 버튼이 `/api/dhcp/simulate-client`(server.ts 341행)를 호출해 **가짜 리스(lease)를 리스트에 밀어 넣는 것**이 현재 동작의 전부다. 실제 DHCP 프로토콜 통신은 전혀 일어나지 않는다.

**개선 방향 — 반드시 결정이 필요한 부분**

"실제 DHCP를 할당된 단말이 표기" 요구사항은 구현 방식에 따라 범위가 크게 달라진다.

| 방식 | 설명 | 난이도/리스크 |
|---|---|---|
| **A. 실제 DHCP 서버 구현** | 이 PC가 실제로 UDP 67/68 포트에서 DORA(Discover-Offer-Request-Ack) 프로토콜을 처리하는 진짜 DHCP 서버가 되어, 같은 네트워크의 실제 단말에 IP를 임대 | 관리자 권한 필요, 기존 네트워크(공유기 등)에 이미 DHCP 서버가 있다면 **충돌 시 다른 기기 인터넷 장애 유발 가능** — 반드시 격리된 테스트망에서만 사용해야 함. `dhcp`(npm) 같은 라이브러리 필요. 구현 난이도 높음 |
| **B. 실제 네트워크 스캔/조회 방식** | DHCP 프로토콜을 직접 구현하지 않고, 현재 PC가 통신한 적 있는 주변 기기를 `arp -a`(ARP 캐시) 또는 네트워크 스캔(ping sweep + ARP)으로 조회해 "연결된 단말" 목록으로 표시 | 관리자 권한 불필요, 다른 네트워크에 영향 없음, 구현 난이도 중간. 다만 엄밀히는 "DHCP 임대"가 아니라 "현재 네트워크에서 보이는 단말" 목록 |

**권장**: 실제 운영 네트워크에 영향을 주지 않는 **B안**을 우선 구현하고, A안(진짜 DHCP 서버)은 격리망에서만 쓸 수 있음을 명확히 안내하는 옵션 기능으로 별도 진행. (이 부분은 구현 전 최종 확인 필요)

**공통 작업**
- `simulationPresets` 배열과 "1클릭 시뮬레이트" UI, `/api/dhcp/simulate-client` 엔드포인트 삭제.
- 리스 목록(`leases`)을 실제 조회 결과로만 채우도록 변경.

---

## 3. TFTP/FTP 공유 폴더 경로 고정 → 수정 가능하도록 변경

**원인**
- `FileServer.tsx`(90~97행)의 폴더 경로 입력창이 `readOnly` + `cursor-not-allowed`로 **UI에서부터 잠겨 있음**.
- 설령 UI를 풀어도 소용없다: 서버의 모든 파일 처리 로직(`/api/files`, 업로드/다운로드 시뮬레이션, 삭제 등, server.ts 530~630행)이 `tftpFtpConfig.rootFolder` 설정값을 **무시하고** 하드코딩된 상수 `SERVED_FOLDER`(29행, `cwd/served_folder` 고정)만 사용한다. `/api/tftpftp/config`(519행)는 `rootFolder`를 받아서 저장은 하지만 실제로 쓰이는 곳이 없다.

**개선 방향**
- `FileServer.tsx`의 폴더 경로 입력을 편집 가능하게 변경 + "저장" 시 서버로 전송.
- 서버 측 `/api/tftpftp/config`에서 경로 유효성 검증 추가: 절대경로인지, 실제 존재하는 디렉터리인지, 쓰기 권한이 있는지 확인 후 저장(존재하지 않으면 생성 여부를 묻거나 에러 반환).
- `/api/files`, 업로드/다운로드/삭제 등 모든 파일 라우트에서 하드코딩된 `SERVED_FOLDER` 대신 **현재 저장된 `tftpFtpConfig.rootFolder`를 참조**하도록 변경.
- 보안: 사용자가 입력한 경로가 시스템 중요 경로(`C:\Windows` 등)를 가리키지 않도록 최소한의 안전장치 검토.

---

## 4. 터미널 자동화(CRT) — 실제 SSH/Telnet 접속 + 실시간 뷰

**원인**
- `/api/scripts/execute`(server.ts 679~764행)는 실제 접속을 전혀 하지 않는다. `setInterval`로 1초마다 명령어를 하나씩 "실행된 것처럼" 진행시키며, `vlan `, `show version` 등 **미리 정해둔 패턴에 매칭되면 캔(canned) 응답 텍스트를 로그에 밀어 넣는 방식**이다. 대상 호스트가 실제로 존재하는지, 접속 가능한지조차 검증하지 않는다.
- 프론트엔드(`TerminalAutomation.tsx`)는 이 가짜 로그를 1.5초 주기 폴링(`App.tsx` 101~108행)으로 받아 텍스트 콘솔처럼 보여줄 뿐, 실제 터미널 세션이 아니다.

**개선 방향**
- 백엔드에 실제 SSH 클라이언트(`ssh2` npm 패키지)와 Telnet 클라이언트(`telnet-client` npm 패키지, 또는 `net` 모듈 기반 직접 구현)를 도입해 `host.protocol`에 따라 실제로 대상 장비에 접속.
- 접속 성공/인증 실패/타임아웃/네트워크 unreachable 등 **실제 에러를 그대로 로그에 기록**해서 왜 접속이 안 되는지 바로 보이도록 처리(현재는 이런 실패 케이스 자체가 존재하지 않음).
- "실제 뷰 형태"로 보여달라는 요청은 실시간성이 핵심이므로, 기존 REST 폴링 방식 대신 **WebSocket**으로 SSH/Telnet 세션의 출력을 실시간 스트리밍하고, 프론트엔드는 `xterm.js`(실제 터미널 에뮬레이터 라이브러리)로 렌더링하는 방식을 권장. 이렇게 하면 SecureCRT/PuTTY와 유사하게 실제 터미널 화면을 그대로 보게 된다.
- 스크립트에 정의된 명령어들을 실제 세션에 순서대로 전송하고, 각 명령의 실제 응답을 그대로 로그/화면에 남긴다(현재처럼 명령어 패턴 매칭으로 가짜 응답을 만드는 로직은 전부 제거).

**단계 제안**
1. (핵심) 백엔드를 실제 SSH/Telnet 접속으로 교체하되, 기존 폴링 구조(`scriptExecutions.logs` 배열 + 1.5초 폴링) 안에서 실제 로그를 채우는 방식으로 우선 구현 — UI 변경 최소화, "진짜 접속 여부/에러"를 바로 확인 가능해짐.
2. (확장) WebSocket + `xterm.js` 기반 실시간 터미널 뷰로 고도화 — 실제 터미널을 보는 것 같은 경험 제공.

---

## 필요한 신규 의존성

| 패키지 | 용도 |
|---|---|
| `ssh2` | 실제 SSH 클라이언트 연결 |
| `telnet-client` | 실제 Telnet 클라이언트 연결 |
| `ws` | WebSocket 서버(실시간 터미널 스트리밍용, 4번 확장 단계에서 필요) |
| `xterm` (`@xterm/xterm`) | 프론트엔드 실제 터미널 렌더링(4번 확장 단계에서 필요) |

---

## 권장 진행 순서 (난이도 낮은 순)

1. **DHCP 어댑터 표시 수정** (1번) — 기존 로직 일부 수정, 리스크 낮음
2. **TFTP/FTP 폴더 경로 편집 가능화** (3번) — UI + 서버 라우트 수정, 리스크 낮음
3. **터미널 자동화 실제 SSH/Telnet 접속 (1단계: 폴링 유지)** (4번) — 신규 의존성 추가, 중간 난이도
4. **1클릭 시뮬레이션 제거 + 실제 단말 조회(B안)** (2번) — 네트워크 스캔 로직 신규 구현, 중간 난이도
5. **터미널 자동화 실시간 뷰 고도화 (WebSocket + xterm.js)** (4번 확장) — 아키텍처 변경 포함, 난이도 높음
6. *(선택, 별도 확인 후)* **실제 DHCP 서버 구현(A안)** — 격리망 전제, 난이도/리스크 가장 높음

## 검증 방법

- 1번: 실제 PC의 Wi-Fi/이더넷 어댑터 이름이 드롭다운에 정확히 표시되고 선택되는지, DHCP 시작 시 실제 IP/MAC이 리스에 찍히는지 확인.
- 3번: 임의의 실제 폴더 경로로 변경 후 저장 → 해당 경로에 파일을 넣었을 때 `/api/files` 목록에 즉시 반영되는지 확인.
- 4번(1단계): 존재하지 않는 IP/틀린 포트로 호스트 등록 후 스크립트 실행 → 실제 연결 실패 에러가 로그에 나타나는지 확인. 실제 접속 가능한 장비(또는 테스트용 SSH 서버)로 정상 접속 및 명령 실행 결과가 실제 응답과 일치하는지 확인.
- 2번: 시뮬레이션 프리셋 UI가 사라졌는지, 네트워크상의 실제 기기가 목록에 나타나는지 확인.
